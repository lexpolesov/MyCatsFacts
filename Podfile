# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MyCatsFacts' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'RealmSwift', '~> 3.12'
  pod 'Kingfisher', '~> 5.0'
  # Pods for MyCatsFacts

  target 'MyCatsFactsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MyCatsFactsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
